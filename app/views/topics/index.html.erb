<div class="fire">
  <!-- NHO: should this header be in the application.html.erb so its on every page?  -->
  <header>
    <% if !current_user %>
    <section class="login">
      <%= button_to "Sign Up", new_user_registration_path %>
      <%= button_to "Sign In", new_user_session_path %>
    <% else %>
    <section class="logout">
    <a href="/topics/new">Comment on a Topic</a>
      <%= button_to "Sign Out", destroy_user_session_path, :method => :delete %>
        <%= current_user.email  %>
    <% end %>
    </section>
  </header>
  <div class="front">
    <h1>F i r e s i d e</h1>
    <h2>Chat with your Member of Congress</h2>
  </div>
</div>
